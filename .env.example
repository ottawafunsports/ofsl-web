# Supabase Configuration
VITE_SUPABASE_URL=https://api.ofsl.ca
VITE_SUPABASE_ANON_KEY=your-anon-key

# Stripe Configuration
VITE_STRIPE_PUBLISHABLE_KEY=your-publishable-key

# Admin Only Mode
# Set to "true" to enable admin-only mode (all routes require admin login)
# Set to "false" for live mode (public access to most routes)
VITE_ADMIN_ONLY_MODE=true